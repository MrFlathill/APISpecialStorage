const express = require('express');
const router = express.Router();
const {} = require('./tank.controller');
//router.get('/', listTanksAction);
//router.get('/:id', viewTankAction);
//router.post('/', insertTankAction);
module.exports = router;